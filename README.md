# Waimarino Shears — Speed Shear Entries

Online entry-management system for Speed Shear competitions using the Waimarino Shears timing/entry platform.

## Live site

`https://entries.waimarinoshears.com`

Visible system name: **Speed Shear Entries**

Private organiser/admin area: **Entry Manager**

Public side: **Speed Shear Competitor Entry**

## Start here in a new ChatGPT/Codex session

Before making changes, read these files in this order:

1. `README.md`
2. `PROJECT_STATE.md`
3. `CHANGELOG.md`

`PROJECT_STATE.md` is the authoritative current-state handoff. `CHANGELOG.md` records completed changes by date.

**Maintenance rule:** every meaningful functional, workflow, deployment, URL, data-model, email, privacy or policy change must update `PROJECT_STATE.md` and `CHANGELOG.md` before the work is considered finished. Update this README when public behaviour, architecture or deployment instructions change.

`IMPLEMENTATION-NOTES.md` is retained only as a compatibility pointer. Do not treat older implementation notes as the current production state.

## Purpose

The system lets a competition organiser manage entries for one Speed Shear competition while Waimarino Shears provides/operates the system.

The competition organiser is responsible for competition administration, including:

- competitor enquiries;
- entry changes/cancellations;
- entry fees/payments;
- check-in/attendance;
- confirming who is ready for the draw.

Waimarino Shears is the system provider/operator and receives backup information where required.

## How competitions are created

The separate Booking Pack/Booking Receiver creates each competition automatically after a booking request is submitted.

Source Booking Pack repository:

`Turiedmonds/waimarino-shears-speed-shear-booking-pack`

The Booking Receiver sends an authorised setup payload containing the Booking Reference, competition details, selected competition contact, grades/events and Programme of Events.

The Entry Manager backend creates one competition record in Google Drive and generates:

- a private organiser/Entry Manager token;
- a public competitor-entry token;
- short private/public links for distribution.

## Link structure

Current custom domain:

`https://entries.waimarinoshears.com`

Short links generated by the backend use:

- private manager: `m.html?c=<short-code>`
- public competitor form: `e.html?c=<short-code>`

The short code resolves server-side to the full random token.

Legacy full-token links remain supported. Old `turiedmonds.github.io/speed-shear-roster-builder/...` links already sent before the custom-domain change redirect to the custom domain and continue to work.

## Private Entry Manager

The private organiser page currently supports:

- competition details loaded from the booking;
- grades/events;
- Programme of Events viewer;
- manual competitor entry;
- bulk list entry;
- online/public entries appearing in the same grade tables;
- competitor phone/email in a Details view rather than the main table;
- Confirmed / Not Confirmed status;
- public entries open/close controls;
- optional overall/custom public closing time;
- fallback final shutdown 12 hours after competition day ends;
- per-grade public open/close controls;
- optional per-grade entry limits;
- grade reordering;
- grade collapse/expand;
- JSON/PDF roster output/submission workflow.

The internal backend field `checkedIn` is retained for compatibility even where the organiser-facing wording is `Confirmed`.

## Public competitor entry

The public form collects:

- competitor name;
- hometown;
- grade/event;
- mobile number and/or email address;
- privacy acknowledgement.

At least one contact method is required.

Privacy version currently in production:

**28 August 2026**

On successful submission:

- the entry is saved centrally;
- an entry reference is assigned, e.g. `WS-2026-0016-E003`;
- if the competitor supplied email, they receive an automatic entry receipt;
- the competition organiser receives a New Competitor Entry email when an organiser email exists;
- Waimarino Shears receives the notification as a backup copy when applicable;
- the organiser remains the contact for changes/cancellations/payments/check-in.

## System Operator Portal

A private, read-only **System Operator Portal** is now implemented in repository source under:

`operator-portal/`

Its purpose is to give Waimarino Shears one permanent operator page to see and open all existing Speed Shear competitions without searching booking emails.

The portal does **not** create a second competition database. It reads the same existing Google Drive JSON competition records from the `Waimarino Speed Shear Entry Manager` folder.

The portal shows:

- competition name/date/venue/Booking Reference;
- organiser contact details;
- total entries and Confirmed / Not Confirmed counts;
- per-grade counts, limits and submitted state;
- public entries Open / Closed;
- roster submission state;
- private Entry Manager link;
- public competitor-entry link;
- Today / Upcoming / Past lifecycle status;
- search/filter/refresh controls.

Architecture/security:

- portal source remains in this repository;
- portal runs as a **separate Google Apps Script web app/project** from the public Entry Manager backend;
- it uses `DriveApp` server-side to read the existing records directly;
- it does not require the Booking Receiver ↔ Entry Manager shared secret;
- no permanent secret is stored in GitHub or browser JavaScript;
- the operator web-app deployment must be restricted to the authorised Waimarino Shears Google account/operator;
- do **not** deploy the operator portal as an unrestricted `Anyone` web app.

Portal source files:

- `operator-portal/google-apps-script/Code.gs`
- `operator-portal/google-apps-script/Index.html`
- `operator-portal/README.md`

Current portal state as at 28 August 2026:

- repository source implemented;
- separate Apps Script project/web-app deployment not yet created;
- therefore the operator portal is **not yet live**.

## Backend

Google Apps Script project:

**Speed Shear Entry Manager**

Current verified live deployment as at 28 August 2026:

**Version 4**

Main Apps Script source files:

- `google-apps-script/WebApp.gs` — router, setup authorisation and short-link resolution
- `google-apps-script/EntryManager.gs` — competition records, links, entry settings and central data operations
- `google-apps-script/EntryManagerV3.gs` — current roster submission/PDF behaviour
- `google-apps-script/CompetitorEntryV4.gs` — public entry, privacy, receipts and organiser notification email

Required Script Property:

- `ENTRY_MANAGER_SHARED_SECRET`

Never commit or document the secret value.

## Frontend files

- `index.html` — redirects into the Entry Manager while preserving query/hash
- `entry-manager.html` / `entry-manager.js` / `entry-manager.css` — private manager
- `competitor-entry.html` / `competitor-entry.js` / `competitor-entry.css` — public entry form
- `m.html` — short private-link resolver
- `e.html` — short public-link resolver
- `entry-manager-workflow.js` / `.css` — submission workflow
- `entry-manager-tidy.js` / `.css` — UI tidy/polish layer
- `entry-manager-drag-autoscroll.js` — drag/reorder assistance
- `CNAME` — custom GitHub Pages domain

## Google Apps Script deployment

When Apps Script source changes:

1. Use the complete current repository file rather than ad-hoc partial edits where possible.
2. Replace the corresponding live file in the **Speed Shear Entry Manager** Apps Script project.
3. Save to Drive.
4. Open **Deploy → Manage deployments**.
5. Edit the active web app deployment.
6. Choose **New version**.
7. Deploy while retaining the existing web-app URL.
8. Update `PROJECT_STATE.md` and `CHANGELOG.md` with the live version.

The System Operator Portal is a separate Apps Script project and has its own deployment instructions in `operator-portal/README.md`.

## Important current limitation

The private manager's `postManager()` currently uses `fetch(..., mode:'no-cors')` and therefore cannot read/verify the backend response body. This was retained during the current implementation but should be improved in a future robustness pass.

Do not assume a manager-side save was centrally confirmed merely because a `no-cors` request did not throw locally.

## Verified production test

Competition used for the latest end-to-end test:

- **Speedshear o ngā Taniwha**
- Booking Reference **WS-2026-0016**
- 18 September 2026
- Turangawaewae marae

Verified:

- private competition link loads correct data on the custom domain;
- public entry link loads correct competition/grade counts;
- online entries save and appear in the manager;
- competitor receipt email works;
- organiser notification email works;
- Waimarino Shears backup email works;
- old GitHub private/public links redirect correctly to the custom domain.

## Next planned project step

Deploy and test the new private **System Operator Portal** Apps Script project using an appropriately restricted Google-account access setting. Do not weaken the access setting merely to make the page reachable.
